# Skill: API Design

Contains the OpenAPI specification for GraphEd.

Rules:
- Backend code should map to documented endpoints.
- Request/response payloads must be schema-validated.
- OpenAPI is a strict contract for the active version.
- Contract changes are allowed only through explicit version bump and changelog updates.
