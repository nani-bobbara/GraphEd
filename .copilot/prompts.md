# Prompting Defaults

When generating code for GraphEd:
- Follow existing service boundaries and naming.
- Produce minimal, production-oriented scaffolding.
- Include input/output types and validation paths.
- Treat OpenAPI as a strict contract for the current version.
- Use Supabase Edge Functions with TypeScript for backend runtime targets.
- Do not bypass validators in AI-driven flows.
